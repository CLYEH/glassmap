# GlassMap data

GeoJSON datasets bundled with the app and served as static files under
`/data/*.geojson`. All fetching happens at preparation time only, via the
scripts in `scripts/`; the running app never calls Overpass or any other
external data API.

## Source and licence

Five of the six files are derived from **OpenStreetMap**, © OpenStreetMap
contributors, available under the [Open Database Licence
(ODbL)](https://opendatacommons.org/licenses/odbl/). Any redistribution of
this data (including this repository) must carry the same attribution.

`listings.geojson` is **fabricated sample data** generated by
`scripts/make-listings.mjs` for demo purposes. It is not derived from OSM
and does not represent real properties, addresses or prices.

## Export details

- Export date: **2026-08-28**
- Overpass endpoint: `https://overpass-api.de/api/interpreter` (POST,
  `[out:json][timeout:90]`), with `https://overpass.kumi.systems/api/interpreter`
  as a fallback mirror. Requests retry on HTTP 429/504 with exponential
  backoff (see `scripts/fetch-osm.mjs`).
- Bounding box (Taipei City, approx.): `lng 121.45–121.67, lat 24.96–25.21`.
  Overpass bbox order is `south,west,north,east` → `24.96,121.45,25.21,121.67`.
- Coordinates rounded to 5 decimals (~1 m). Polygons simplified with
  `@turf/turf` `simplify` (Douglas-Peucker, `highQuality: true`) and clipped
  to the bounding box with `bboxClip`. Tolerance is `0.00012` for parks
  (needed to stay under the file-size budget — see "Parks" below) and
  `0.00003` for districts (only 12 features, so they can afford much finer
  detail — see "Districts" below, T-24). Because each district/park polygon
  is simplified independently, shared borders between adjacent districts may
  show gaps/overlaps at this zoom level — see "Districts" below for why this
  is a source-data property rather than mainly a simplification artefact —
  acceptable for the demo's visual use, not survey-grade.
- Regenerate: `node scripts/fetch-osm.mjs` (writes the five OSM-derived
  files) then `node scripts/make-listings.mjs` (writes `listings.geojson`,
  which depends on `mrt-stations.geojson` and `districts.geojson`). Pass
  `--only=<comma-separated names>` (e.g. `--only=districts`) to regenerate a
  subset without touching the others; valid names are `mrt`, `districts`,
  `parks`, `schools`, `supermarkets`.
  Re-running hits live OSM data and reproduces the committed files
  byte-for-byte only if upstream OSM data has not changed since the export
  date above.

## Files

| File | Category | Feature count | Size | Geometry | Overpass query |
|---|---|---:|---:|---|---|
| `mrt-stations.geojson` | `mrt_station` | 109 | 30.7 KB | Point | see below |
| `districts.geojson` | `district` | 12 | 61.1 KB | Polygon | see below |
| `parks.geojson` | `park` | 865 | 287.5 KB | Polygon/MultiPolygon | see below |
| `schools.geojson` | `school` | 445 | 101.5 KB | Point (centroid) | see below |
| `supermarkets.geojson` | `supermarket` | 607 | 128.7 KB | Point (centroid) | see below |
| `listings.geojson` | `listing` | 25 | 4.8 KB | Point | fabricated, not OSM |

Total: 614.3 KB (well under the 1 MB combined budget; every file is under
the 300 KB per-file target).

### MRT stations

```
[out:json][timeout:90];
(
  node["railway"="station"]["station"="subway"](24.96,121.45,25.21,121.67);
  node["railway"="station"]["subway"="yes"](24.96,121.45,25.21,121.67);
  node["railway"="station"]["network"~"Taipei Metro|臺北捷運|Taipei Rapid Transit"](24.96,121.45,25.21,121.67);
);
out body;
```

Taipei Metro maps each station as several nodes (one per entrance/platform
sharing the same `name`). These are **deduped by `name`**: all nodes
sharing a name are averaged into a single point, and the feature id is
taken from the lowest OSM node id in the group (deterministic across
re-runs). `tags.network` / `tags.operator` are kept when present.

The bbox query is not restricted to Taipei City administratively, so it
also picks up 12 stations tagged `network=New Taipei Metro` (the Circular
Line, `operator=New Taipei Metro Corporation`). More broadly, 36 of the
109 stations fall geographically outside the 12 district polygons in
`districts.geojson` (checked with `turf.booleanPointInPolygon`) - near-
border stations operated by Taipei Rapid Transit Corporation but sited in
neighbouring New Taipei City, in addition to the 12 New Taipei Metro ones.

### Districts

```
[out:json][timeout:90];
rel(id:2782528,2783061,2822029,2822030,2869465,2881027,2881028,2881029,2881105,2905064,2905065,2905066);
out geom;
```

Taipei City's 12 districts are tagged `boundary=administrative`,
**`admin_level=7`** in OSM (the city itself, "臺北市", would be
`admin_level=6`, but no such relation intersected the query bbox in a
usable way; districts are pinned by explicit relation id instead of area
name, which also sidesteps the "臺北市" vs "台北市" character-variant
ambiguity in `area["name"=...]` lookups). Determined by querying every
`admin_level` 6/7 relation inside a wider Taipei-area bbox and confirming
all matches came back `admin_level=7`, then identifying the 12 relations
whose `name:en` values are the 12 Taipei City districts.

Multipolygon ways are joined into closed rings in `fetch-osm.mjs`
(`assembleRings`/`buildPolygon`) from the `out geom;` member geometry, then
clipped to the bbox and simplified. Only Beitou actually touches the clip
line: its northern tip runs into the Yangmingshan area, and 134 m of its
boundary (two adjacent vertices at `lat=25.21`) sits exactly on the north
edge of the bbox. Shilin, despite also having hilly terrain in the north,
tops out at `lat=25.1954` and never reaches any bbox edge, so it is not
clipped at all. This follows the task's explicit "clip to it" instruction
over showing the full administrative extent.

`id` format: `district:<english-slug>` (e.g. `district:daan`,
`district:zhongzheng`), derived from the OSM `name:en` tag.

#### Simplify tolerance (T-24)

Districts use simplify tolerance **`0.00003`** (`DISTRICT_SIMPLIFY_TOLERANCE`
in `fetch-osm.mjs`), not the `0.00012` shared by parks. With only 12
features at 61.1 KB, districts have plenty of headroom under the 300 KB
per-file budget to keep far more boundary detail than parks need to.

This tolerance was tightened while investigating a tool-review report that
`describe_surroundings` named a Taipei district for a New Taipei location -
specifically, that Wanhua's polygon crossed the Xindian river and contained
"Banqiao Station" at `121.4933,25.0143`, ~393 m inside the boundary.
Re-verifying that report surfaced two things worth recording so they aren't
rediscovered the same way twice:

1. **The reported coordinate is not Banqiao Station.** OSM's own Nominatim
   reverse geocoder places `121.4933,25.0143` at 下庄子 (Xiazhuangzi),
   Dongyuan village, **Wanhua District, Taipei** - a real Taipei
   neighbourhood, not New Taipei. The actual Banqiao Station (verified the
   same way, Nominatim search for 板橋車站/"Banqiao Station" ->
   `121.4618,25.0132`, "MRT Banqiao Station Exit 1, Banqiao District, New
   Taipei") was already outside every district polygon, at every tolerance
   tested from `0.00012` down to near-zero (i.e. the raw, unsimplified OSM
   ring) - so there was no reproducible overshoot at the real station.
   Containment of the reported coordinate is unaffected by tolerance (still
   ~386 m inside Wanhua even unsimplified) because it is correct: that point
   really is in Wanhua. The regression test added for this task (in
   `src/lib/data/data.test.ts`) uses the verified real Banqiao Station
   coordinate instead of the one in the original report.
2. **The ~150 m gaps between adjacent district borders are a source-data
   property, not mainly a simplification artefact.** Re-simplifying at
   tolerances from `0.00012` down to near-zero left the largest gap between
   any two adjacent districts' vertices essentially unchanged (~150 m
   throughout - e.g. `0.00012`: 149.6 m, `0.00003`: 147.8 m, near-zero:
   149.4 m, all between the same `neihu`/`shilin`/`zhongshan`/`datong`
   vertices). Each district relation was traced independently in OSM and
   their shared borders do not actually share way nodes even in the raw
   `out geom` response, so simplify tolerance - which only bounds how far a
   simplified line strays from *its own* original line - cannot close a gap
   that already exists between two *different* original lines. Closing it
   would need topology-aware simplification across adjacent polygons
   (snapping shared vertices before or during simplify), which is out of
   scope for this task's per-feature-independent pipeline.

The tighter tolerance is still kept because finer per-district boundary
detail is a real (if modest) improvement on its own, well within the
suggested `0.00002`-`0.00004` range, and it does not regress anything: file
size stays small, no polygon gained a kink (`turf.kinks` reports zero for
all 12 features), Taipei 101 still resolves to exactly `district:xinyi`, and
Daan Forest Park still resolves to exactly `district:daan`.

Regenerate districts only (leaving the other four OSM-derived files
untouched) with `node scripts/fetch-osm.mjs --only=districts`.

### Parks

```
[out:json][timeout:90];
(
  way["leisure"="park"](24.96,121.45,25.21,121.67);
  way["leisure"="garden"](24.96,121.45,25.21,121.67);
  relation["leisure"="park"](24.96,121.45,25.21,121.67);
  relation["leisure"="garden"](24.96,121.45,25.21,121.67);
);
out geom;
```

Polygons clipped to the bbox, then those with area `< 2000 m²` (computed
with `@turf/turf` `area`) are dropped. 865 remaining features is the
biggest file by far because Taipei has a very large number of small
neighbourhood parks and gardens above that threshold; simplify tolerance
was raised to `0.00012` (top of the suggested `0.00005–0.0001` range plus
a small margin) specifically to keep this file under 300 KB.

### Schools

```
[out:json][timeout:90];
(
  node["amenity"="school"](24.96,121.45,25.21,121.67);
  way["amenity"="school"](24.96,121.45,25.21,121.67);
  relation["amenity"="school"](24.96,121.45,25.21,121.67);
);
out center;
```

All matches are kept (no filtering). Ways/relations use the Overpass
`out center;` centroid. `tags.level = "elementary"` is set when the name
matches `/國小|國民小學/`, `tags["school:type"] === "primary"`, or
`tags.isced` includes level `1`. Because the query is bbox-based (not
clipped to the district polygons), a handful of schools just across the
Taipei City border in neighbouring New Taipei City districts may appear —
this matches the task's bbox-based instruction for this dataset.

### Supermarkets

```
[out:json][timeout:90];
(
  node["shop"="supermarket"](24.96,121.45,25.21,121.67);
  way["shop"="supermarket"](24.96,121.45,25.21,121.67);
  relation["shop"="supermarket"](24.96,121.45,25.21,121.67);
);
out center;
```

Same centroid approach as schools. Dominated by PX Mart (208), Simple Mart
(143) and Carrefour (89 combined, across a shorter and a longer OSM name
variant) — consistent with known Taipei supermarket density.

### Listings (fabricated)

`scripts/make-listings.mjs` generates 25 points scattered 50–300 m from a
seeded, deterministic selection of MRT stations located inside Daan,
Zhongshan, Songshan and Xinyi districts (membership tested with
`turf.booleanPointInPolygon` against `districts.geojson`). Each scatter is
re-drawn (same seeded RNG, so still deterministic) up to 20 times until
the point itself also lands inside one of those four districts, since a
station near a border can otherwise scatter across it; the station's own
coordinate is used as a last-resort fallback. Every feature has `sample:
true`, `source: "sample"`, and a name of the form `"Sample listing 07"` —
deliberately not a real address or price. Re-running the script with the
same seed (`20260828`) reproduces byte-identical output.

## Property shape

Every feature's `properties` follows `GlassMapFeatureProperties` in
`src/lib/data/schema.ts`:

- `id` — `osm:<node|way|relation>:<osm_id>` for OSM data, `listing:<n>` for
  sample listings, `district:<slug>` for districts. Unique across all six
  files (2063 features total, checked by `scripts/fetch-osm.mjs` and again
  by hand across all files including `listings.geojson`).
- `name` — local-language name (zh-TW). Untrusted content when surfaced by
  tools.
- `nameEn` — from OSM `name:en` when present.
- `category` — one of the `FEATURE_CATEGORIES` in `schema.ts`.
- `source` — `"osm"` or `"sample"`.
- `sample` — `true` only on `listings.geojson`.
- `tags` — a small allow-listed subset of source tags (`network`,
  `operator`, `level`, `admin_level`); never depended on by tools, only
  for display.

## Tier-2 categories (T-60)

`public/data/tier2/<category>.geojson` — 18 additional OSM point-of-interest
categories, generated by `scripts/fetch-tier2.mjs`, kept separate from the
six tier-1 files above (different property shape — `id`, `name`, `category`,
optional `nameEn` (from OSM `name:en`, matching tier-1's
`GlassMapFeatureProperties.nameEn` convention), `cuisine`, `brand`,
`opening_hours`, plus the enrichment fields added in T-97 — `address`,
`phone`, `website`, `wheelchair`, and five category-specific fields — see
"Enrichment fields (T-97)" below for the full list and provenance — no
polygons, much higher feature counts for some categories).
`public/data/tier2/index.json` is a
manifest (`{ generated, attribution, categories: [{ category, count, file,
bytes }] }`) so the tool layer can read per-category counts without loading
every file.

### Source, licence and export details

Same source as tier-1: **OpenStreetMap**, © OpenStreetMap contributors,
[ODbL](https://opendatacommons.org/licenses/odbl/). Same redistribution
attribution requirement applies.

- Export date: **2026-08-31** (T-97 re-export; the original tier-2 export was
  **2026-08-30** — see "Enrichment fields (T-97)" below for what changed)
- Overpass endpoints, in try order: `https://overpass-api.de/api/interpreter`,
  `https://overpass.kumi.systems/api/interpreter` (both reused from
  `fetch-osm.mjs`), plus a third mirror added for this export,
  `https://overpass.openstreetmap.fr/api/interpreter` — see "Overpass
  availability during this export" below for why. `[out:json][timeout:180]`,
  retry on HTTP 429/504 with exponential backoff, `POST`.
- Bounding box: same as tier-1, `lng 121.45–121.67, lat 24.96–25.21`
  (`24.96,121.45,25.21,121.67` in Overpass's south,west,north,east order).
- Query shape, one per category: `nwr["<key>"="<value>"](bbox); out center;`
  — nodes use their own coordinate, ways/relations use Overpass's `out
  center` centroid. Coordinates rounded to **6 decimals** (~11 cm; tier-2's
  contract specifies 6dp, unlike tier-1's 5dp).
- Unnamed features (no `name` tag) are **skipped entirely**.
- Regenerate: `node scripts/fetch-tier2.mjs` (all 18 categories, then writes
  `index.json`). `--only=<comma-separated categories>` fetches a subset
  without touching the others or rewriting the manifest (useful for
  investigating one category, or resuming after a partial failure — the
  script does not abort the whole run when one category's Overpass query
  ultimately fails on every endpoint; it logs `FAILED`, continues with the
  rest, and prints the exact `--only=...` command to retry the failed ones).
  `--manifest-only` rebuilds `index.json` from the 18 files already on disk
  without querying Overpass at all (used after a set of `--only` runs
  finishes covering all categories, so the large categories that already
  succeeded — e.g. `restaurant` at 2.4 MB — are not re-fetched just to
  regenerate the manifest).

### Categories and tag mapping

| Category | Overpass tag | Count | Size |
|---|---|---:|---:|
| `restaurant` | `amenity=restaurant` | 13789 | 3001.2 KB |
| `cafe` | `amenity=cafe` | 2298 | 573.6 KB |
| `fast_food` | `amenity=fast_food` | 1288 | 364.3 KB |
| `bakery` | `shop=bakery` | 497 | 110.7 KB |
| `bar` | `amenity=bar` | 205 | 46.2 KB |
| `convenience` | `shop=convenience` | 3231 | 803.8 KB |
| `pharmacy` | `amenity=pharmacy` | 563 | 122.6 KB |
| `clinic` | `amenity=clinic` | 864 | 187.6 KB |
| `hospital` | `amenity=hospital` | 74 | 21.6 KB |
| `place_of_worship` | `amenity=place_of_worship` | 1718 | 372.1 KB |
| `bank` | `amenity=bank` | 1576 | 515.8 KB |
| `hotel` | `tourism=hotel` | 576 | 150.5 KB |
| `parking` | `amenity=parking` | 986 | 199.9 KB |
| `bicycle_rental` | `amenity=bicycle_rental` | 2602 | 685.1 KB |
| `library` | `amenity=library` | 209 | 51.9 KB |
| `museum` | `tourism=museum` | 119 | 35.2 KB |
| `post_office` | `amenity=post_office` | 254 | 94.7 KB |
| `police` | `amenity=police` | 220 | 55.7 KB |

Sizes above (and the total below) are generated from
`public/data/tier2/index.json` — the manifest's `bytes`/`count` fields are
this table's source of truth; a regression test in `data.test.ts` checks
they still match the files on disk.

Total: 31069 features, 7,569,794 bytes (7.22 MB raw) / ~1.07 MB gzipped
(static hosting serves these compressed; the `< 300 KB per file` target in
the top-level data rules was written for tier-1's much sparser categories
and does not hold for Taipei's real POI density — six of the eighteen
files exceed it: `restaurant`, `bicycle_rental`, `convenience`, `cafe`,
`bank` and `place_of_worship` — flagged here rather than silently
exceeded). The raw/gzipped totals grew by +18.4%/+33.3% over the
pre-T-97 export (6,392,787 bytes raw / ~0.84 MB gzipped) from the added
enrichment fields below; feature counts are otherwise unchanged
except `cafe` (+1, ordinary upstream OSM edits between the two export
dates, not a bug in this pipeline).

Notes on specific counts:

- **`restaurant` (13789) and `convenience` (3231)** match the plausible
  Taipei ranges given for this task (~12–15k and ~3k respectively).
- **`bicycle_rental` (2602)** is YouBike 2.0 stations (`brand=YouBike 2.0`
  on every sampled feature) plus a handful of Danhai LRT-adjacent stations
  just across the bbox's New Taipei edge — consistent with YouBike's very
  high station density in the metro area, not a bug.
- **`police` (220, 209 unique names)** is higher than a rough "dozens"
  guess but checks out: Taipei's 12 police precincts (分局) each run many
  local sub-stations (派出所), plus MRT police, highway police and a few
  named units, and — like tier-1's schools/supermarkets — the bbox query
  is not clipped to the district polygons, so a few near-border stations
  in neighbouring New Taipei City (e.g. 板橋派出所) are included. Verified
  by inspecting all 209 names; none looked spurious.
- **12 ids appear in two category files** (e.g. `osm:node:3929888360`
  "Mister Donut" as both `bakery` and `fast_food`; a few bakery/restaurant
  and hotel/restaurant combos). These are real OSM features carrying two
  matching tags simultaneously (e.g. `shop=bakery` and `amenity=fast_food`
  on the same node) — each category file is generated independently from
  its own tag query, so the same physical place can legitimately appear in
  more than one file with the same `id`. Not a duplicate-id bug within any
  single file (checked: none).

### Enrichment fields (T-97)

Beyond the base `cuisine`/`brand`/`opening_hours` set, `fetchCategory` in
`scripts/fetch-tier2.mjs` also carries these OSM tags when present, kept as
plain strings with no further normalisation:

- **`address`** — one composed line. `addr:full` is used verbatim when
  present (it is already composed by the OSM contributor and often keeps a
  postcode prefix that only `addr:full` carries, e.g.
  `"111台北市士林區德行西路5號"`); otherwise built from `addr:city` +
  `addr:district` + `addr:street` + `addr:housenumber`, concatenated in that
  order with no separators — the order Taiwanese addresses read, largest
  administrative unit first — which matches every `addr:*` sample checked for
  this bbox (Han-script throughout; no `addr:city`/`addr:street` value in
  Latin script turned up in samples across restaurants, hotels, pharmacies,
  hospitals, places of worship and parking). `addr:housenumber` occasionally
  already ends in `號` (e.g. `"60號"`); it is appended only when missing.
  Capped at 120 characters.
- **`phone`** — `phone`, falling back to `contact:phone` when `phone` is
  absent.
- **`website`** — `website`, falling back to `contact:website`; kept only
  when the value starts with `http://` or `https://` (drops bare social
  handles and other non-URL values seen on some features).
- **`wheelchair`** — kept only when the OSM value is exactly `yes`, `no` or
  `limited` (drops other free-text values such as `designated`).
- Five fields extracted only for the one category each is meaningful for,
  even when the raw tag happens to appear on a feature elsewhere: `stars`
  (hotel), `fee` + `capacity` (parking), `dispensing` (pharmacy), `religion`
  + `denomination` (place_of_worship), `emergency` (hospital). Capped at 60
  characters each.

Coverage varies widely by category and field. Citywide across all 31069
tier-2 features: `address` ~43%, `phone` ~22%, `wheelchair` ~14%, `website`
~9%; the category-specific fields are naturally rarer citywide since each
only applies within its own category (e.g. `religion` is on 1089/1718
`place_of_worship` features, `fee` on 642/986 `parking` features). Coverage
is markedly lower here than a small central-Taipei sample would suggest,
since the citywide query also reaches far less densely-tagged
outer-district POIs.

### Overpass availability during this export

The primary endpoint (`overpass-api.de`) began refusing TCP connections
outright partway through this export — its own fair-use throttle, most
likely tripped by tier-2's much heavier request volume than tier-1 (the
`restaurant` query alone returned 13.8k elements / 2.6 MB, fetched shortly
before the failures started). The `kumi.systems` mirror was independently
returning HTTP 500 for unrelated trivial queries at the same time, so it
provided no fallback either. A third mirror,
`overpass.openstreetmap.fr`, was verified with `curl` outside the script
(confirmed current, non-empty data for this bbox) and added as a fallback
before the remaining 11 categories were fetched successfully through it.

One candidate mirror, `overpass.osm.ch`, was tried and rejected: it
returned HTTP 200 with **zero elements** for every query tested, including
a trivial `node["amenity"="police"]` query — a country/region-scoped
instance silently missing Taipei entirely, which is worse than an explicit
failure because it looks identical to a genuinely empty category. As a
result `fetchCategory`'s caller now treats `count === 0` as a validation
problem (logged, not silently written as `OK`) specifically to catch this
failure mode if it recurs.

`ENDPOINTS` in `fetch-tier2.mjs` keeps the official instance first (as
`fetch-osm.mjs` does) so a future re-run tries it before falling back —
this export's connection refusals may not persist.

**Update, T-97 re-export (2026-08-31):** they did not — every one of the 18
categories again exhausted all 4 attempts on `overpass-api.de` (connection
failures) and all 4 on `overpass.kumi.systems` (HTTP 500/502/timeout) before
succeeding on the first attempt through `overpass.openstreetmap.fr`, with no
exceptions. The third mirror is, in practice, now this pipeline's only
working endpoint rather than a fallback; `ENDPOINTS`' ordering is left
unchanged here since re-ordering it is a behaviour change outside this
task's scope, but a future data task should consider trying
`overpass.openstreetmap.fr` first.
